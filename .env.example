# Copy to .env: `cp .env.example .env`
#
# These are the local dev credentials from compose.yaml, not secrets — that file
# is committed and contains the same values. If the two ever disagree,
# compose.yaml wins, because POSTGRES_* only takes effect on `initdb`.
DATABASE_URL=postgres://cairn:cairn@localhost:5433/cairn
